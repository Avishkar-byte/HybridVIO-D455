<launch>
    <!-- Argument to reset camera on start -->
    <arg name="initial_reset" default="false"/>

    <!-- 1. RealSense Driver (IMAGES + DEPTH + RGB) -->
    <!-- CRITICAL: IMU Disabled here. Handled by Python Node -->
    <include file="$(find realsense2_camera)/launch/rs_camera.launch">
        <arg name="align_depth" value="true"/>
        
        <!-- Enable RGB and Depth as requested -->
        <arg name="enable_color" value="true"/>
        <arg name="enable_depth" value="true"/>
        
        <!-- Enable Stereo IR for VINS -->
        <arg name="enable_infra1" value="true"/>
        <arg name="enable_infra2" value="true"/>
        <arg name="infra_width" value="848"/>
        <arg name="infra_height" value="480"/>
        <arg name="infra_fps" value="30"/>
        
        <!-- DISABLE Driver IMU (We use custom node) -->
        <arg name="unite_imu_method" value=""/> 
        <arg name="enable_gyro" value="false"/>
        <arg name="enable_accel" value="false"/>
        
        <!-- Synchronization -->
        <arg name="enable_sync" value="true"/>
        
        <!-- Do NOT force reset here to avoid conflict race, or let user decide via arg -->
        <!-- <arg name="initial_reset" value="$(arg initial_reset)"/> --> 
    </include>

    <!-- 2. Custom IMU Node (Python) -->
    <node name="d455_imu_publisher" pkg="vio_launch" type="imu_publisher_node.py" output="screen" />

    <!-- 3. VINS-Fusion Estimator (Stereo + IMU) -->
    <node name="vins_estimator" pkg="vins" type="vins_node" args="$(find vio_launch)/config/realsense_d455_hybrid.yaml" output="screen" />
</launch>
